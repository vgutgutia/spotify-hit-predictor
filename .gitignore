# Data (Kaggle raw CSVs + combined are large — don't commit)
data/*.csv
data/*.zip
data/LICENSE
data/README.txt
!data/.gitkeep

# EDA / feature importance plots regenerated by preprocess.py and train.py
report/

# Models — tracked (small enough to ship with the repo for HF Spaces deploy).
# If we add large intermediate dumps later, ignore them specifically here.

# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
.pytest_cache/

# Envs
.env
.venv/
venv/
env/

# Streamlit
.streamlit/secrets.toml

# OS / IDE
.DS_Store
.vscode/
.idea/
